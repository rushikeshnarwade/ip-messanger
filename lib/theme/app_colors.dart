import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0D1117); // Deep blue/gray
  static const Color primary = Color(0xFF1F6FEB);    // Vibrant blue
  static const Color secondary = Color(0xFF58A6FF);  // Light blue (outgoing messages)
  static const Color surface = Color(0xFF161B22);    // Incoming messages
}
